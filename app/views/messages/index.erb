<% @messages.each do |message| %>
  <div class="message">
    <h3><a href="messages/<%= message.id %>"><%= message.content %> </a><small>(by: <%=message.author %>)</small><% if message.url %> <a href="<%=message.url %>">link</a></h3>
    <% end %>
  </div>
<% end %>